server:
  port: 8761
spring:
  application:
    name: center
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:${server.port:8761}/eureka/


